swagger: "2.0"
info:
  description: This is the swagger file that goes with our server code
  version: "1.0.0"
  title: Swagger REST Article
consumes:
  - "application/json"
produces:
  - "application/json"

# basePath: "/api"

# Paths supported by the server application
paths:
  /stack_overflow/api/v1/auth/login:
    get:
      operationId: "models.verify_login"
      tags:
        - "Users"
      summary: "The people data structure supported by the server application"
      description: "Login"
      responses:
        200:
          description: "Successful Login"
          schema:
            type: "array"
            items:
              properties:
                message:
                  type: "string"
                access_token:
                  type: "string"
